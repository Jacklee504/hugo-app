# Project Agent Notes

- After adding or updating deal files in `content/deals/*.md`, always run:
  - `python3 scripts/sync_listing_from_urls.py`
- Purpose: ensure `listing_image` and related listing metadata are populated so images appear immediately on localhost.
